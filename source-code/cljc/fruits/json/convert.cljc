
(ns fruits.json.convert)

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

(defn to-clj
  ; @param (json) n
  ;
  ; @usage
  ; (to-clj ...)
  ; =>
  ; {...}
  ;
  ; @return (map)
  [n]
  #?(:cljs (js->clj n :keywordize-keys true)))
